**单继承对象的内存布局**，第一个为虚函数表指针vtbl,其后为成员且先基类后子类；虚函数表里包含了所有的虚函数地址，以NULL结束，虚函数如果子类有重写，就由子类的重新代替。

**多重继承对象的内存布局**，一个虚表，按照继承的声明顺序排列基类地址，其中第一个基类地址不变，后面的基类的地址有偏移。

**虚拟继承**：**使公共的基类在子类中只有一份**，

- 第一种处理情况：每一个对象必须为其每一个虚基类背负一个额外的指针。

- 第二种处理情况：只增加一个指针，指针指向一个增加的虚表，虚表存放这些额外的指针。
